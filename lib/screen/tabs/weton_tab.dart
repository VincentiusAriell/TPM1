import 'package:flutter/material.dart';

class WetonTab extends StatelessWidget {
  const WetonTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Halaman Weton"),
    );
  }
}